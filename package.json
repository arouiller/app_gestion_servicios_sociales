{
  "name": "app_gestion_servicios_sociales",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "node backend/src/index.js"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
