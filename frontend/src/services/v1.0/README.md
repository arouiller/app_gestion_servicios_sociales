# Services v1.0

Servicios HTTP para modelo 1.0.x.

## Archivos

- `personasService.js` — GET /api/personas?search=...
- `recibosService.js` — POST /api/recibos/generar, GET /api/recibos
- `planesService.js` — CRUD /api/planes (modelo 1.0.x)
